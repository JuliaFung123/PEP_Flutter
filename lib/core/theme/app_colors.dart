import 'package:flutter/material.dart';

/// Brand and semantic colors for the KPI app.
abstract final class AppColors {
  static const Color seed = Color(0xFF006494);
  static const Color secondarySeed = Color(0xFF247BA0);
}
